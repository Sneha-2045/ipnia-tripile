# Production frontend build (Vite)
# Used when running `npm run build` / GitHub Actions deploy.
# Do NOT put secrets or Duffel keys here.
# Supabase keys are injected via GitHub Actions secrets (see deploy.yml).

VITE_API_URL=https://ipnia-tripile.onrender.com
VITE_CASHFREE_MODE=production
